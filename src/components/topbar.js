// Topbar component (scaffold)
// وظیفه: نوار بالایی شامل عنوان، جستجو و اعلان‌ها

export function mountTopbar(rootSelector = '.topbar'){
  const root = document.querySelector(rootSelector);
  if(!root) return;
  // placeholder — topbar عناصر تعاملی را مدیریت می‌کند
}
